# PAC-direct blink on the chiptool PAC (no HAL, plain cortex-m). Separate
# member so no svd2rust PAC enters the graph — see embassy-pac/Cargo.toml.
[package]
name = "examples-pac-chiptool"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
cortex-m.workspace = true
cortex-m-rt.workspace = true
critical-section.workspace = true
cxd56-pac-chiptool.workspace = true
panic-halt.workspace = true
vcell.workspace = true
