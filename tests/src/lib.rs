//! Support crate for the on-hardware tests. The real tests are the
//! harness=false integration tests in tests/*.rs and the plain-defmt bins in
//! src/bin/ — see README.md and `cargo xtask test-all`. Intentionally empty.
#![no_std]
